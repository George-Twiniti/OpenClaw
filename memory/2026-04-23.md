# 2026-04-23
- Exchange/Graph startup brief is now working again after fixing `app_settings.exchange_mailbox_email` to `george.broadbent@twiniti.ai`.
- Exchange settings are stored in Neon `second-brain.app_settings` and the startup brief should use DB-sourced Exchange creds/identity, not the stale `Secrete.txt` path.
- The Exchange auth flow is application-only Graph auth; `/me` is not valid and the mailbox must be queried via `/users/<mailbox>`.
- The inbox/calendar issue was caused by an invalid Exchange mailbox value in the DB, not a permission problem.
- Current Exchange DB keys now present: `exchange_tenant_id`, `exchange_client_id`, `exchange_client_secret`, `exchange_mailbox_email`.
- Gmail email/calendar continued working during the repair.
- The startup brief now returns Exchange unread mail and next-24h events successfully.
- Remaining good practice: keep `Secrete.txt` out of the startup brief path for Exchange.
- Built a two-way Tailnet bridge for transcript jobs between Oracle and the local machine.
- Live bridge URLs: Oracle `http://vnic002.taild942c7.ts.net:8787`; local `http://gbroadbent5-1.taild942c7.ts.net:8788`.
- The missing piece during troubleshooting was the actual local job API; status-only shells were not enough.
- GitHub push closeout was incomplete earlier in the day: OpenClaw push was blocked by a secret in history, and Second-Brain push needed a rebase. That blocker should be remembered as a real unfinished task from this session.
