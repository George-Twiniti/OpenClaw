## 2026-03-26
- Oracle server (129.159.181.219): Telegram integration still returning the same error as of 2026-03-26 13:28 Europe/London.
- Memory system issue: `memory_search` unavailable because Ollama embeddings model `mxbai-embed-large` is missing (HTTP 404); fix is likely `ollama pull mxbai-embed-large` or adjust embeddings config.
- Oracle server config: removed OpenAI as a model provider (removed `openai:default` auth profile and removed `openai/*` models from agent fallbacks), keeping `openai-codex` OAuth as the only cloud fallback.
