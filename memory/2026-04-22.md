# 2026-04-22
- Morning startup brief: open loop remains the Oracle-side Telegram/router runtime layer that still leaks `skill_update` wrappers into Telegram replies.
- Also still open: verify end-to-end YouTube/video processing writes transcripts/summaries to the canonical Second-Brain paths.
- Missing file note: `second-brain/references/daily-wrapup-spec.md` was not found.
# 2026-04-22
- Morning startup brief: open loop remains the Oracle-side Telegram/router runtime layer that still leaks `skill_update` wrappers into Telegram replies.
- Also still open: verify end-to-end YouTube/video processing writes transcripts/summaries to the canonical Second-Brain paths.
- Missing file note: `second-brain/references/daily-wrapup-spec.md` was not found.
- Oracle gateway troubleshooting: the live blocker shifted from pending approval/pairing to a read-only gateway capability; gateway health is otherwise OK, but runtime capability remains read-only.
- Local pairing state on Oracle was edited so `paired.json` now reflects expanded operator scopes and `pending.json` was cleared, but the service still reports read-only capability.
- Service/auth checks on Oracle showed token auth mode is enabled in config, but the gateway still resolves to read-only at runtime, suggesting the remaining issue is in auth/capability bootstrap rather than the visible config files.
