# 2026-05-21
- Updated `skills/video-transcript-downloader/scripts/vtd.js` so YouTube transcript extraction tries Firecrawl first via the `firecrawl` CLI, then falls back to `youtube-transcript-plus` and yt-dlp subtitles.
- Updated `skills/video-transcript-downloader/SKILL.md` to document Firecrawl as the primary transcript extractor.
- Firecrawl CLI is installed at `/home/george/.npm-global/bin/firecrawl`.
- A smoke test against a sample YouTube URL started but did not complete quickly enough to give a clean end-to-end confirmation, so the code path is wired but still needs a live transcript-capable page to validate fully.
- Confirmed on `https://youtu.be/zP6TnEiueEc?si=AvdglQCRdXERS6qQ` that the Firecrawl path returns transcript text; the helper writes a transcript file and metadata JSON successfully for that video.
- User wants computer-sent videos handled by extracting the transcript and sending the transcript-focused result back to Computer.
- Checked `state/local-video-transcript-queue.json` and the local bridge inbox on 2026-05-21; all known transcript jobs were already completed, so there was nothing new to process.
- Checked  and the local bridge inbox on 2026-05-21; all known transcript jobs were already completed, so there was nothing new to process.
- When a fresh Computer-sent video arrived on 2026-05-21, no new queued transcript job was visible in `state/local-video-transcript-queue.json`, so the system appears to have fallen back to a stub instead of a live transcript run.
