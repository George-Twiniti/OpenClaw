# 2026-05-20
- Patched `telegram-video-processing` so transcript extraction now uses Firecrawl first, via `firecrawl scrape` in `scripts/youtube_transcript.py`.
- The helper now saves a stub note when Firecrawl doesn't surface transcript text, instead of archiving the whole page body as a fake transcript.
- Verified the Telegram wrapper script still compiles with `python3 -m py_compile`.
- In practice, Firecrawl on the sample YouTube link returned page metadata but no transcript text, so the Telegram helper now saves a stub note unless Firecrawl actually exposes transcript content.
- The working Firecrawl transcript path is `scrape` plus a JS click on YouTube's `Transcript` tab, then a short wait; that yields a transcript section in the returned markdown.
