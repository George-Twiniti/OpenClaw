# 2026-04-25
- Oracle gateway health issue traced to Tailscale serve sidecar failing with `spawn tailscale ENOENT` inside the systemd service environment.
- Oracle gateway service needs `/snap/bin` in its systemd PATH for the serve sidecar to find `tailscale`.
- Gateway bind should remain `loopback` when using Tailnet serve; the failure is the serve sidecar path, not bind mode.
- Oracle gateway default model was updated to `openai-codex/gpt-5.4-mini`.
- Local node pairing/connectivity eventually reached a connected state after starting the node host, but Oracle endpoint health remained separate and still needed fixing.
- Second-Brain was rebased and pushed successfully; OpenClaw remained dirty and was intentionally not pushed because of live state such as secrets, memory files, and a submodule pointer.
