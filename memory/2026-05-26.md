# 2026-05-26
- Checked the transcript bridge queue during the cron reminder.
- Local bridge status was clean: `0 queued / 3 total / 3 results`.
- `state/local-video-transcript-queue.json` contained only completed jobs; there were no new transcript items to process.
- `scripts/oracle-transcript-pending.json` was empty.
- A remote bridge sync probe hit a `fetch failed` error, but the local queue/result state was still healthy.
- Re-checked the live bridge/status endpoints later in the same wake.
- `scripts/oracle-bridge-sync.mjs status` reported `{"queued":0,"total":3,"results":3}`.
- `scripts/oracle-transcript-bridge.mjs drain` failed because the remote endpoint returned HTML instead of JSON.
