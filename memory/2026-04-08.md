# 2026-04-08

- Fixed the Oracle server’s swap situation by creating and enabling an 8 GiB `/swapfile`.
- Confirmed the Oracle server should be kept as an orchestration / worker host, not as the primary fast interactive inference box.
- Verified Ollama on Oracle is using `qwen3:4b` locally; `qwen3.5:cloud` is a remote tag placeholder.
- Measured local Ollama latency on Oracle at roughly 57 seconds for a trivial prompt, which is too slow for interactive chat but acceptable for batch/background work.
- Confirmed Telegram integration is already present on Oracle and that the host is appropriate for Telegram video processing.
- Confirmed the Second Brain workflow exists and includes daily summaries, handoff files, and video transcript/summary paths.
- Tomorrow’s follow-up: verify whether the daily Second Brain synthesis job is actually scheduled/automated or merely documented as a workflow.
