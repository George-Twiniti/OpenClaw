# 2026-03-23

- Built OpenClaw Setup product pack: offer, checklist, prereqs, case study template, Gumroad copy, covers + MP4 demo; drafted Systeme.io landing page HTML and a snippet without html/head/body tags (for openclawsetup.twiniti.ai).
- Second‑Brain Starter expanded with Slack Lite (no n8n) webhook receiver + Local Dashboard; added prereq doc (Cloudflare Tunnel + Slack app for Slack Lite) and updated Gumroad copy; refreshed ZIP.
- Slack Lite plan: workspace Twiniti, channel #daily-log; requires public webhook via Cloudflare Tunnel once gateway is on a public server.
- Enabled compaction pre‑flush memory and updated cron jobs to decrypt encrypted gmail.env with AGE key at /home/george/.openclaw/keys/age.key before running startup brief/bills scan (fixes Telegram summary error).
- Processed additional YouTube video (Anthropic /loop) into transcript + creator‑ready summary and saved to Second‑Brain.
- Built out the Second‑Brain data layer on Neon/Postgres: applied schema for captures, summaries, pgvector embeddings, and graph tables; created MCP server + run.sh key-loading path; ingested 49 OpenClaw AI conversation blocks into Postgres; added graph ingestion and updated the dashboard to show DB captures + graph overview.
- Captured Substack posts from George Broadbent’s Substack into `second-brain/conv/substack/` using the Substack archive API, including full-text post bodies.
- Set OpenClaw model priority to task-aware fallback order: `openai-codex/gpt-5.4` → `openai/gpt-5.2` → `ollama:qwen3.5:0.8b`.
- Provisioned Oracle Ubuntu server (`129.159.181.219`) as the future 24x7 OpenClaw host: verified SSH access, installed Node 22 + OpenClaw natively, enabled UFW with only SSH open, enabled fail2ban, tightened `/home/ubuntu/.openclaw` to 700, and removed passwordless sudo for `ubuntu`.
